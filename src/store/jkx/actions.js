import * as types from './mutations_types'
export default {
    ////【例子】:异步操作在action中，然后提交到mutations
    // clear_local: ({
    //     commit
    // }) => {
    //     commit(types.CLEAR_LOCAL)
    // },
}